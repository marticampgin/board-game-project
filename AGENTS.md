# Working agreement

Read `shattered_realm_codex_master_spec.md` before changing game rules. Preserve its LOCKED choices and record prototype assumptions in `docs/DECISIONS.md`.

Keep authoritative gameplay in RefCounted domain classes. UI, CLI and bots submit the same commands. Never consume gameplay RNG in presentation.

Always commit changes with useful descriptions. Stage only your own assigned files when working concurrently. Run relevant headless tests and launch checks before a milestone is declared complete.

